apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vm-portal-db
  namespace: vm-portal
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-storage    # Delete policy — swap to nfs-datasets for Retain
  resources:
    requests:
      storage: 2Gi
